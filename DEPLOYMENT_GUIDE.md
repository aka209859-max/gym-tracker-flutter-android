# 🚀 GYM MATCH デプロイ + テストユーザー招待ガイド

このドキュメントは、GYM MATCHアプリをCloudflare Pagesにデプロイし、テストユーザーを招待する完全な手順を提供します。

---

## 📋 目次

1. [デプロイ準備](#デプロイ準備)
2. [Cloudflare Pagesデプロイ](#cloudflare-pagesデプロイ)
3. [Firebase設定](#firebase設定)
4. [テストユーザー招待方法](#テストユーザー招待方法)
5. [トラブルシューティング](#トラブルシューティング)

---

## 🎯 デプロイ準備

### ステータス確認

✅ **完了項目**:
- Flutter build web 完了（build/web/）
- Git commit完了（34a52df）
- GitHub push完了（gym-tracker-flutter）
- Wrangler CLI インストール完了

### 必要なもの

1. **Cloudflareアカウント**（無料）
   - URL: https://dash.cloudflare.com/sign-up
   
2. **Firebase Console アクセス**
   - URL: https://console.firebase.google.com/
   - プロジェクト: 既存のFirebaseプロジェクト

---

## 🌐 Cloudflare Pages デプロイ

### 方法1: GitHub連携デプロイ（推奨）⭐

**手順**:

1. **Cloudflare Dashboardにアクセス**
   ```
   https://dash.cloudflare.com/
   ```

2. **Pages > Create a project**
   - "Connect to Git" を選択
   - GitHub アカウントを連携

3. **リポジトリ選択**
   - リポジトリ: `gym-tracker-flutter`
   - ブランチ: `main`

4. **ビルド設定**
   ```
   Framework preset: None (手動設定)
   Build command: (空欄)
   Build output directory: build/web
   Root directory: /
   ```

5. **環境変数設定**（オプション）
   ```
   NODE_VERSION: 18
   ```

6. **Deploy site** をクリック

7. **デプロイURL取得**
   - 例: `https://gym-tracker-flutter.pages.dev`
   - カスタムドメイン設定可能

### 方法2: Wrangler CLIデプロイ

**手順**:

```bash
# 1. Cloudflare認証（初回のみ）
wrangler login

# 2. Pagesプロジェクト作成
wrangler pages project create gym-match

# 3. デプロイ実行
cd /home/user/flutter_app
wrangler pages deploy build/web --project-name=gym-match

# 4. デプロイURL確認
# 出力例: https://gym-match.pages.dev
```

**後続デプロイ**:
```bash
cd /home/user/flutter_app
flutter build web --release
wrangler pages deploy build/web --project-name=gym-match
```

---

## 🔥 Firebase設定

### 1. Firebase Authentication設定

**手順**:

1. **Firebase Console にアクセス**
   ```
   https://console.firebase.google.com/
   ```

2. **プロジェクト選択** → **Authentication**

3. **Sign-in method タブ**
   - "Email/Password" を有効化
   - "Email link (passwordless sign-in)" は無効のまま

4. **Authorized domains に追加**
   - Cloudflare PagesのURL（例: `gym-match.pages.dev`）
   - カスタムドメインがあれば追加

### 2. Firestore Database確認

**確認項目**:
- ✅ Firestoreデータベースが作成済み
- ✅ Security Rulesが設定済み
- ✅ `users` コレクションが存在

**Security Rules更新**（必要に応じて）:
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Campaign applications
    match /campaign_applications/{applicationId} {
      allow read: if request.auth != null && resource.data.user_id == request.auth.uid;
      allow create: if request.auth != null && request.resource.data.user_id == request.auth.uid;
      allow update: if request.auth != null && resource.data.user_id == request.auth.uid;
    }
  }
}
```

### 3. Firebase Web App設定確認

**確認手順**:
1. Firebase Console → Project Settings → General
2. "Your apps" セクション → Web app
3. SDK setup and configuration を確認
4. `lib/firebase_options.dart` の内容と一致していることを確認

---

## 👥 テストユーザー招待方法

### 方法1: 招待リンク共有（推奨）⭐

**準備**:

1. **デプロイURL確認**
   - 例: `https://gym-match.pages.dev`

2. **招待メッセージテンプレート作成**

```
【GYM MATCH テストユーザー募集】

NexaJPの新アプリ「GYM MATCH」のテストユーザーを募集しています！

🎯 アプリURL:
https://gym-match.pages.dev

✅ テスト内容:
- アプリの動作確認
- UI/UXフィードバック
- バグ報告

🎁 特典:
- 先行アクセス
- プレミアムプラン 2ヶ月無料（リリース後適用）

📝 アカウント作成手順:
1. 上記URLにアクセス
2. 「アカウント作成」をタップ
3. メールアドレスとパスワードを入力
4. 登録完了！

ご協力よろしくお願いします！
NexaJP CEO
```

### 方法2: Firebase Test Lab（上級者向け）

**手順**:

1. **Firebase Console** → **Test Lab**
2. デバイスとOSバージョン選択
3. テストシナリオ作成
4. 実行とレポート確認

### 方法3: 直接招待（少人数向け）

**手順**:

1. **テストユーザーリスト作成**
   ```
   - 山田太郎 (yamada@example.com)
   - 佐藤花子 (sato@example.com)
   - 鈴木一郎 (suzuki@example.com)
   ```

2. **個別に招待メール送信**

3. **フィードバック収集フォーム準備**
   - Google Forms
   - Typeform
   - Notion

---

## 📱 テストユーザー用ガイド

以下のテキストをテストユーザーに共有してください：

```markdown
# GYM MATCH テストユーザーガイド

## アカウント作成

1. アプリURL: https://gym-match.pages.dev
2. 「アカウント作成」をタップ
3. メールアドレスとパスワードを入力
4. 登録完了

## テスト項目

### 基本機能
- [ ] アカウント作成（メール認証）
- [ ] ログイン/ログアウト
- [ ] プロフィール編集

### サブスクリプション
- [ ] プラン一覧表示（無料/プレミアム/Pro）
- [ ] 価格表示（¥500/¥980）
- [ ] 乗り換え割キャンペーンボタン

### キャンペーン機能
- [ ] 乗り換え元アプリ選択
- [ ] ユニークコード表示
- [ ] SNSテンプレートコピー
- [ ] SNSシェア機能

### UI/UX
- [ ] 全体的なデザイン
- [ ] ボタンの押しやすさ
- [ ] 文字の読みやすさ
- [ ] ナビゲーションの分かりやすさ

## バグ報告

見つけたバグは以下の形式で報告してください：

**発生場所**:
**操作内容**:
**期待される動作**:
**実際の動作**:
**スクリーンショット**:
**デバイス情報**:
```

---

## 🎯 テストユーザー管理

### Firestore でユーザー確認

**手順**:

1. **Firebase Console** → **Firestore Database**
2. **users コレクション** を開く
3. テストユーザーのドキュメントを確認

**確認項目**:
```javascript
{
  uid: "abc123...",
  email: "test@example.com",
  displayName: "テスト太郎",
  createdAt: Timestamp,
  subscription: {
    type: "free",
    campaign_benefit_applied: false
  }
}
```

### テストユーザー識別

**方法1: 専用コレクション作成**

```javascript
// Firestore: test_users コレクション
{
  uid: "abc123...",
  email: "test@example.com",
  role: "tester",
  invitedBy: "CEO",
  invitedAt: Timestamp,
  testGroup: "alpha"
}
```

**方法2: メールドメイン識別**

```dart
bool isTestUser(String email) {
  return email.endsWith('@test.nexajp.com') ||
         email.endsWith('@example.com');
}
```

---

## 📊 テストフェーズ計画

### Phase 1: アルファテスト（社内・知人）

**期間**: 1週間
**人数**: 5-10人
**目的**: 重大バグ発見、基本動作確認

**チェックリスト**:
- [ ] アカウント作成が正常動作
- [ ] サブスクリプション画面表示
- [ ] キャンペーン登録フロー
- [ ] データ保存/読み込み

### Phase 2: ベータテスト（限定公開）

**期間**: 2週間
**人数**: 50-100人
**目的**: UXフィードバック、負荷テスト

**チェックリスト**:
- [ ] 同時アクセス処理
- [ ] キャンペーン自動検証
- [ ] Firebase連携
- [ ] レスポンス速度

### Phase 3: パブリックベータ（一般公開）

**期間**: 1ヶ月
**人数**: 無制限
**目的**: 本番運用前の最終確認

**チェックリスト**:
- [ ] スケーラビリティ
- [ ] 長期運用安定性
- [ ] ユーザーフィードバック反映

---

## 🔧 トラブルシューティング

### よくある問題

#### 1. デプロイが失敗する

**原因**: ビルドファイルが存在しない

**解決策**:
```bash
cd /home/user/flutter_app
flutter build web --release
wrangler pages deploy build/web --project-name=gym-match
```

#### 2. Firebase認証エラー

**エラー**: `auth/unauthorized-domain`

**解決策**:
1. Firebase Console → Authentication → Settings
2. Authorized domains にCloudflare URLを追加
   - 例: `gym-match.pages.dev`

#### 3. Firestoreアクセスエラー

**エラー**: `permission-denied`

**解決策**:
1. Firebase Console → Firestore → Rules
2. Security Rulesを確認・更新
3. ルールのデプロイ

#### 4. ユーザーがログインできない

**確認項目**:
- [ ] Firebase Authentication有効化
- [ ] メール/パスワード認証有効化
- [ ] Authorized domains設定
- [ ] ネットワーク接続

---

## 📈 メトリクス監視

### Firebase Analytics設定

**手順**:

1. **Firebase Console** → **Analytics**
2. **Events** タブで以下を確認
   - `sign_up`: 新規登録数
   - `login`: ログイン数
   - `campaign_start`: キャンペーン開始数
   - `campaign_complete`: キャンペーン完了数

### Cloudflare Analytics

**確認項目**:
- ページビュー数
- ユニークビジター数
- 地域別アクセス
- デバイス別アクセス

---

## 🎉 デプロイ完了チェックリスト

### 必須項目

- [ ] Flutter build web 成功
- [ ] Cloudflare Pages デプロイ完了
- [ ] デプロイURL確認（例: `https://gym-match.pages.dev`）
- [ ] Firebase Authentication 有効化
- [ ] Authorized domains 設定
- [ ] Firestore Security Rules 更新
- [ ] テストユーザー招待メッセージ準備

### オプション項目

- [ ] カスタムドメイン設定
- [ ] Google Analytics連携
- [ ] Sentryエラー監視
- [ ] カスタムエラーページ
- [ ] PWA機能有効化

---

## 📞 サポート連絡先

**CEO**: ceo@nexajp.com

**GitHub Repository**: https://github.com/aka209859-max/gym-tracker-flutter

**Firebase Project**: [プロジェクト名]

---

## 🚀 次のステップ

1. **デプロイ完了後**:
   - テストユーザー5-10人に招待リンク送信
   - フィードバック収集

2. **1週間後**:
   - バグ修正
   - UX改善

3. **2週間後**:
   - ベータテスト拡大（50-100人）
   - キャンペーン自動化テスト

4. **1ヶ月後**:
   - 一般公開準備
   - マーケティング開始

---

**CEO、「48時間で"勘"を"確信"に変える」NexaJPの最初の実演が、今、世界に公開されます！** 🚀💪
